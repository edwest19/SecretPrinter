<!--
  SecretPrinter.Dns.csproj

  Written by Claude (Anthropic model, Claude Opus 4.5) at the direction of
  Edwin West, for the SecretPrinter project.

  The one and only DNS wire-format implementation in this repository. Every
  other component references this project rather than carrying its own copy,
  so that an auditor has exactly one parser to review.

  No NuGet package references: base class library only, so there is no
  third-party code in the parsing path.
-->
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>SecretPrinter.Dns</RootNamespace>
    <AssemblyName>SecretPrinter.Dns</AssemblyName>
  </PropertyGroup>

</Project>
